# mcp-agent Secrets Configuration for Lynx AI
# Copy this file to mcp_agent.secrets.yaml and update with your actual keys
# This file should be in .gitignore and never committed

# OpenAI API Key
openai:
  api_key: "${OPENAI_API_KEY}"  # Or set directly: "sk-..."

# Anthropic API Key (optional)
# anthropic:
#   api_key: "${ANTHROPIC_API_KEY}"

# Google API Key (optional)
# google:
#   api_key: "${GOOGLE_API_KEY}"

# Note: Lynx-specific secrets (Kernel, Supabase) are loaded separately
# via environment variables or config/config.yaml

