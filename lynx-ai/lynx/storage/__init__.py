"""
Storage backends for Lynx AI.

Supports both in-memory (testing) and Supabase (production) storage.
"""

# Lazy imports to avoid circular dependencies
# Import directly from submodules when needed

